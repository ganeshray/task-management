# Production Environment Variables
NODE_ENV=production
PORT=8000

# Use a strong, secure JWT secret in production
JWT_SECRET="taskmanagementxyz"

# Production MongoDB URI - using the same as dev for now (replace with your production database)
MONGO_URI="mongodb+srv://test_user_001:P0werscript@cluster0.uyhydqz.mongodb.net/task_management_prod?w=majority"

# CORS settings for production (replace with your frontend domain)
# FRONTEND_URL="https://your-frontend-domain.com"
FRONTEND_URL="http://localhost:5173"

# Other production settings
BCRYPT_ROUNDS=12
